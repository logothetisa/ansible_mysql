# -------------------------------------------------------------------------
# Start MySQL
# -------------------------------------------------------------------------
- name: tart mysqld service
  ansible.builtin.systemd:
    name: mysqld
    state: started
  register: start_result

- name: Confirm mysqld started successfully
  ansible.builtin.debug:
    msg: "mysqld service started successfully"
  when: start_result is succeeded

