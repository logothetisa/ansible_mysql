[defaults]
remote_user = ansible
result_format = yaml
deprecation_warnings = False
host_key_checking = False
inventory_ignore_patterns = vars
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False
